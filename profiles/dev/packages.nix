# Dev Profile Packages
#
# This file is intentionally empty. Package decisions belong in the user's
# hydrix-config/profiles/dev/packages.nix.
#
{ config, lib, pkgs, ... }:

{
  environment.systemPackages = [
    # Configured in user's hydrix-config
  ];
}

[] Review README.md, make sure everything is up to date

[] Add in thanks to Redflake, after testing pentest VM declarative BurpSuite setup both and without pro mode

[] Fix License

[] Remove Claude mentions

[] Rename `microvm` CLI command to `mvm` to avoid overriding the upstream microvm command

[] microvmHost.defaultBridge — document what the actual fallback behaviour is when a TAP doesn't match a known profile bridge, and whether this option is user-useful or internal plumbing
